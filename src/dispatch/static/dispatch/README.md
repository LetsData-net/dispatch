# How to Run the Vue

1. Navigate to the directory in the terminal:
```bash
cd src/dispatch/static/dispatch
```

2. Install dependencies using `npm i`

3. Start the project with the command `npm run dev` or `npm run serve`

